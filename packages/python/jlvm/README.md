# constellation-metagraph-sdk-jlvm

**Reserved placeholder distribution.**

This package intentionally contains no runnable source yet. It reserves the
`constellation_metagraph.jlvm` namespace and the `constellation-metagraph-sdk-jlvm`
distribution name for the future Python port of the JLVM evaluator, crypto-opcode
surface, and proof tooling, and is versioned in lockstep with
[`constellation-metagraph-sdk-core`](../core) and
[`constellation-metagraph-sdk`](../main).

There is nothing to import here beyond the package marker:

```python
import constellation_metagraph.jlvm  # placeholder — no public API yet
```
